#include <iostream>
#include <string>
using namespace std ; 

int main(){
    int test , i , j , k , l ; 
    int manny  ;
    cin >> test ; 
    for(i=0 ; i < test ; i++){
        cin >> manny ; 
        string s[manny]  , temp ; 
        for(j =0 ; j < manny ;j++){
            cin >> s[j];
        }
        for(k =0 ; k < manny -1 ; k++){
            for(l = k + 1 ;  l < manny ; l++){
                if(strcmp(s))
            }
        }
    }
}